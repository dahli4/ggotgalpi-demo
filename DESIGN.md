# 꽃갈피 Design System

## 1. Atmosphere & Identity

꽃갈피는 종이에 연필로 기록한 독서 노트처럼 조용하고 따뜻해야 한다. 크림색 바탕과 책별 구분 색만 정체성으로 남기고, 시스템 UI는 콘텐츠보다 앞에 나서지 않는다. iOS 기본 탐색과 컨트롤을 우선 사용하며 장식은 얇은 선으로 제한한다.

## 2. Color

| 역할 | SwiftUI 토큰 | 값 | 사용 |
| --- | --- | --- | --- |
| 기본 배경 | `GgotgalpiTheme.paper` | `rgb(0.978, 0.971, 0.949)` | 앱 배경 |
| 보조 배경 | `GgotgalpiTheme.paperDeep` | `rgb(0.943, 0.929, 0.892)` | 선택 상태, 입력 배경 |
| 기본 텍스트 | `GgotgalpiTheme.ink` | `rgb(0.18, 0.18, 0.17)` | 제목, 본문 |
| 보조 텍스트 | `GgotgalpiTheme.secondaryInk` | `rgb(0.43, 0.41, 0.37)` | 설명, 메타데이터 |
| 구분선 | `GgotgalpiTheme.line` | `rgb(0.84, 0.82, 0.77)` | 얇은 구분선 |
| 강조 | `GgotgalpiTheme.accent` | `rgb(0.46, 0.43, 0.36)` | 선택, 주요 버튼, 포커스 |

- 화면 코드에서는 원시 색상 대신 의미 토큰을 사용한다.
- 책 구분 색은 콘텐츠 데이터이므로 모델에 둘 수 있지만, 시스템 UI 색으로 재사용하지 않는다.
- 강조색은 상호작용과 현재 선택을 알리는 용도로만 사용한다.

## 3. Typography

| 단계 | SwiftUI 스타일 | 사용 |
| --- | --- | --- |
| 화면 제목 | `.largeTitle`, bold | 화면의 가장 큰 제목 |
| 섹션 제목 | `.headline` | 날짜와 콘텐츠 섹션 |
| 항목 제목 | `.headline` | 책과 감상 제목 |
| 본문 | `.body` | 감상문 |
| 보조 본문 | `.subheadline` | 설명과 저자 |
| 메타데이터 | `.caption` | 날짜, 페이지, 개수 |

- Dynamic Type이 동작하도록 의미 기반 시스템 크기를 우선한다.
- 별도 서체를 섞지 않고 시스템 서체의 의미 기반 스타일만 사용한다.
- 본문은 `.footnote`보다 작게 만들지 않는다.

## 4. Spacing & Layout

기본 단위는 4pt다.

| 토큰 | 값 | 사용 |
| --- | --- | --- |
| `compact` | 8pt | 아이콘과 라벨, 짧은 행 |
| `control` | 12pt | 컨트롤 내부 간격 |
| `content` | 16pt | 카드와 목록 내부 여백 |
| `screen` | 20pt | 휴대폰 화면 좌우 여백 |
| `section` | 24pt | 주요 섹션 사이 |
| `largeSection` | 32pt | 화면의 큰 콘텐츠 그룹 |

- 화면의 스크롤 소유자는 하나만 둔다.
- 탭바와 내비게이션바의 안전 영역은 SwiftUI가 관리하며 수동으로 같은 높이의 여백을 추가하지 않는다.
- 캘린더는 화면 너비를 사용하고, 선택한 날짜의 감상은 그 아래 한 열로 배치한다.

## 5. Components

### App Tab

- **구조**: SwiftUI `TabView` + `Label` + SF Symbols.
- **상태**: 기본, 선택. 선택 상태는 시스템 틴트가 담당한다.
- **접근성**: 아이콘만 사용하지 않고 항상 한국어 라벨을 제공한다.
- **레이아웃**: 탭마다 독립적인 `NavigationStack`을 가진 앱 셸.

### Native Calendar

- **구조**: 그래픽 스타일 `DatePicker`와 선택 날짜의 감상 섹션.
- **상태**: 오늘, 선택 날짜, 감상 없음, 감상 1개 이상.
- **접근성**: 한국어 로케일, 시스템 날짜 라벨, Dynamic Type을 유지한다.
- **레이아웃**: 화면 너비의 단일 열. 탭바 안전 영역을 침범하지 않는다.

### Section Label

- **구조**: 시스템 headline과 선택적인 텍스트 액션.
- **상태**: 기본, 액션 있음.
- **접근성**: 액션은 `Button`이며 텍스트 라벨을 가진다.

### Book Color Mark

- **구조**: 책별 콘텐츠 색과 제목 첫 글자를 가진 작은 정사각형 표식.
- **상태**: 목록, 상세, 캘린더 크기.
- **접근성**: 장식으로 숨기고 인접한 전체 제목에서 작품 정보를 제공한다.

### Empty State

- **구조**: SF Symbol, 한 줄 제목, 짧은 안내.
- **상태**: 선택 날짜에 감상이 없을 때 사용한다.
- **접근성**: 색만으로 비어 있음을 표현하지 않는다.

## 6. Motion & Interaction

- 화면 전환과 탭 선택은 iOS 시스템 동작을 사용한다.
- 분야 필터는 시스템 segmented picker의 기본 전환을 사용한다.
- Reduce Motion 설정에서는 시스템이 제공하는 축소 동작을 그대로 따른다.
- 모든 버튼은 최소 44pt 터치 영역을 확보한다.

## 7. Depth & Surface

전략은 `tonal-shift`다. `paper`와 `paperDeep`의 명도 차이, 필요한 곳의 얇은 `line`만 사용한다. 카드 그림자와 입체적인 표지 표현은 사용하지 않는다.

## 8. Accessibility Constraints & Accepted Debt

- iOS의 Dynamic Type, VoiceOver 레이블, 44pt 터치 영역을 기본 요구사항으로 둔다.
- 본문과 배경의 대비를 색상만으로 약화하지 않는다.
- 긴 한국어 작품명과 감상문은 잘리지 않고 세로로 확장되어야 한다.
- 현재 승인된 접근성 부채는 없다.
